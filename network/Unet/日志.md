```
# 2020/11/27晚上对该Unet网络的pytorch代码的实现有点问题，因为对于__init__的层的定义出了问题，
# 因此在网络层的输出时发现网络结构仅仅只有__init__的两个层，因此这里需要我们对这个进行相应的探讨
# 是不是需要将网络层全部定义在__init__中，如果不需要，那为什么最后的网络结构的输出仅仅是__init__的两个
# 如果是，为什么需要，并且需要定义到哪种程度上
# 希望明天2020/11/28将该问题解决

# 2020/11/28日，结合代码将部分进行了修改，但是在进行input时出现了cat的错误，希望在2020/11/29日能够解决
```